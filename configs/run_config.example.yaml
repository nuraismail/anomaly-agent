# Optional run-specific override file.
# Use with: python AnomalyAgent.py --config configs/run_config.example.yaml

agent:
  model: openai/gpt-5.5
  thread_id: example_run
  reasoning_effort: high

test:
  tests_to_run: 1
  max_searches_per_test: 1

plot:
  figure.dpi: 150
  savefig.dpi: 300

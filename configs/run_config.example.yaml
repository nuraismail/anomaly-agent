# Optional run-specific override file.
# Use with: python anomaly_agent.py --config configs/run_config.example.yaml

agent:
  model: openai/gpt-5.5
  thread_id: example_run
  reasoning_effort: high

test:
  tests_to_run: 1
  max_searches_per_test: 1

paths:
  sim_maps_path: data/input/CMBmapsPlanckLCDM256.npy

plot:
  figure.dpi: 150
  savefig.dpi: 300
